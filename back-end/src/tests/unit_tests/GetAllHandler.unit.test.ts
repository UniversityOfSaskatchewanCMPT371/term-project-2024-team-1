import "reflect-metadata";


describe("UserGetAllHandler Tests", () => {
  // registerAllDependencies();
  // const userGetHandler: UserGetAllHandler = container.resolve(UserGetAllHandler);
  describe("handle", () => {
    fit("should return all users if success", async () => {
      // const mockRequest: Request = {
      //   body: {},
      // } as Request;
      // // const mockResponse: any = {
      // //   json: jest.fn(),
      // //   status: jest.fn()
      // // } as Response;

      // const mockResponse: any = jest.spyOn(Response.prototype, "send").mockImplementation(()=>[]);

      // const mockUserService: any = jest.spyOn(UserService.prototype, "getAll");
      // mockUserService.mockReturnValue([]);
      // jest.spyOn(UserGetAllHandler.prototype, "validation").mockReturnValue(true);
      // userGetHandler.handle(mockRequest, mockResponse);

      expect(true).toBeTruthy(); 
    });
  });
});
