// system test stub
describe("System test sample: Class/component name test", () => {
  describe("Class/component method test", () => {
    it("Class component method test case: should do something based on something", () => {
      expect(true).toBeTruthy();
    });
  });
});

// stub for testing server response
/*  import supertest from "supertest";
// @ts-expect-error testing out bypassing error 
import app from "../../../src/server"; 

const request = supertest(app);

describe("Smoke Test", () => {
  it("should respond with status 200", async () => {
    const response = await request.get("/");
    expect(response.status).toBe(200);
  });
});
*/
